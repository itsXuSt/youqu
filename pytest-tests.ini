[pytest]
testpaths = tests
addopts = -s -vv --tb=short --rootdir=tests --confcutdir=tests
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
markers =
    e2e: end-to-end test (requires VLM API access)
    slow: test that takes significant time

